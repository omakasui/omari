#!/bin/bash

# omari:summary=Launch a TUI or focus an existing terminal window for it
# omari:args=<command> [args...]

APP_ID="org.omari.$(basename "$1")"
LAUNCH_COMMAND="omari-launch-tui $@"

exec omari-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
