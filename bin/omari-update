#!/bin/bash

# omari:summary=Update Omaritu and system packages.
# omari:args=[-y]
# omari:examples=omari update | omari update -y
# omari:requires-sudo=true

set -e

trap 'echo ""; echo -e "\033[0;31mSomething went wrong during the update!\n\nPlease review the output above carefully, correct the error, and retry the update.\033[0m"' ERR

if [[ $1 == "-y" ]] || omari-update-confirm; then
  omari-update-git
  omari-update-perform
fi