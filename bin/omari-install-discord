#!/bin/bash

# omari:summary=Install the Discord desktop app.
# omari:requires-sudo=true

echo "Installing Discord..."

cd /tmp
wget https://discord.com/api/download?platform=linux -O discord.deb
sudo apt install ./discord.deb -y
rm discord.deb
cd -
