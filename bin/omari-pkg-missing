#!/bin/bash

# omari:summary=Returns true if any of the named packages are missing from the system (or false if they're all there).
# omari:args=<packages...>

for pkg in "$@"; do
  if ! dpkg-query -W -f='${Status}' "$pkg" 2>/dev/null | grep -q "^install ok installed"; then
    exit 0
  fi
done

exit 1
