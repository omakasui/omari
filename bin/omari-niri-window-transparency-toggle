#!/bin/bash

# omari:summary=Toggles transparency for the currently focused window.
# omari:group=niri

WINDOW_ID=$(niri msg -j focused-window 2>/dev/null | jq -r '.id // empty')

[[ -n $WINDOW_ID ]] && niri msg action toggle-window-rule-opacity --id "$WINDOW_ID" >/dev/null 2>&1
