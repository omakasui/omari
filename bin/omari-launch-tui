#!/bin/bash

# omari:summary=Launch a TUI command in the default terminal.
# omari:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.omari.$(basename $1) -e "$1" "${@:2}"